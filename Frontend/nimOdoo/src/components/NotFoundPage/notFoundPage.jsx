import { FormContainer } from "../UserAccess/FormContainer/formContainer"

export function NotFoundPage ({}) {
    return (
        <FormContainer>
            Error 404 not Found
        </FormContainer>
    )
}